using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FiringController : MonoBehaviour {

    // Calculates heading V3 for aiming with current projectile speed and named target vector
    public Vector3 PredictAim(Vector3 shooter, string targetName, float projectileSpeed) {
        Vector3 aimVector = new Vector3(0f,0f,0f);

        return aimVector;
    }

   


}
